<div class="card">
    <div class="card-body">
        <h1>{{post.titulo}}</h1>
        <hr>
        <small>Data de publicação: {{post.data}}</small><br>
        <hr>
        <p>{{post.conteudo}}</p>
    </div>
</div>